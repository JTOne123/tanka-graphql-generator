directive @asProperty on FIELD_DEFINITION
directive @asAbstract on FIELD_DEFINITION

type Query {
	
}

schema {
	query: Query
}

type Address {
	city: String!
	street: String!
}

type Contact {
	lastName: String!
	firstName: String!
	fullName: String! @asAbstract
	homeAddress: Address @asProperty
	officeAddress: Address
}