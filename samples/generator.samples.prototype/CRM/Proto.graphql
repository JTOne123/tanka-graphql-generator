directive @gen(
	asAbstract: Boolean! = false,
	asProperty: Boolean! = false,
	clrType: String = null
) on FIELD_DEFINITION

type Query {
	field: String
}

schema {
	query: Query
}

type Contact {
	firstName: String
	lastName: String
}