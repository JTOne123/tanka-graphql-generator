type Contact {
    id: ID!
    firstName: String!
    lastName: String!
    address: Address
}

type Address {
    city: String!
    country: String!
	street: String!
}

type Query {
    contact(id: ID!): Contact
}

schema {
    query: Query
}